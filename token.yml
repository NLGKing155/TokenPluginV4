package com.tokenplugin.managers;

import com.tokenplugin.TokenPlugin;
import org.bukkit.configuration.file.FileConfiguration;
import org.bukkit.configuration.file.YamlConfiguration;
import java.io.File;
import java.io.IOException;
import java.util.UUID;

public class TokenManager {
    private final TokenPlugin plugin;
    private File file;
    private FileConfiguration config;

    public TokenManager(TokenPlugin plugin) {
        this.plugin = plugin;
        setup();
    }

    public void setup() {
        if (!plugin.getDataFolder().exists()) plugin.getDataFolder().mkdirs();
        file = new File(plugin.getDataFolder(), "tokens.yml");
        if (!file.exists()) {
            try { file.createNewFile(); } catch (IOException e) { e.printStackTrace(); }
        }
        config = YamlConfiguration.loadConfiguration(file);
    }

    public void addTokens(UUID uuid, String tokenType, int amount) {
        int current = config.getInt(uuid.toString() + "." + tokenType, 0);
        config.set(uuid.toString() + "." + tokenType, current + amount);
        save();
    }

    public int getTokens(UUID uuid, String tokenType) {
        return config.getInt(uuid.toString() + "." + tokenType, 0);
    }

    private void save() {
        try { config.save(file); } catch (IOException e) { e.printStackTrace(); }
    }
}
